# Optimizely SDK Configuration
# Set one of these to enable PollingConfigManager for automatic datafile updates

# Production Environment (used with: npm run start)
# Option 1: Use SDK Key (recommended)
# OPTIMIZELY_SDK_KEY=your_production_sdk_key_here

# Option 2: Use Datafile URL
# OPTIMIZELY_DATAFILE_URL=https://cdn.optimizely.com/datafiles/your_production_datafile_url

# Development Environment (used with: npm run dev)
# Option 1: Use SDK Key (recommended)
# OPTIMIZELY_SDK_KEY_DEV=your_dev_sdk_key_here

# Option 2: Use Datafile URL
# OPTIMIZELY_DATAFILE_URL_DEV=https://cdn.optimizely.com/datafiles/your_dev_datafile_url

# Note: If neither is set, the app will use StaticConfigManager with the embedded datafile

# Optimizely Event Processor Options
# Set to 'forwarding' to send events immediately (useful in development/debug)
# Set to 'batch' to buffer events and send in groups (default for production)
# OPTIMIZELY_EVENT_PROCESSOR=forwarding

# For batch processor tuning (batch size and flush interval in ms)
# OPTIMIZELY_EVENT_BATCH_SIZE=10
# OPTIMIZELY_EVENT_FLUSH_INTERVAL=1000
